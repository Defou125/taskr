services:
    postgres:
      image: postgres:17
      environment:
        POSTGRES_DB: taskr
        POSTGRES_USER: taskr
        POSTGRES_PASSWORD: taskr
      ports:
        - "5432:5432"
      volumes:
        - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
